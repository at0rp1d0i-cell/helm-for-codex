# Quality Bar

## Code

Prefer modular, bounded, readable implementations.

## Tests

Every structural rule added to the repo should have a validation check or test, and behavior-aware verification should include the installed runtime path so future tranches can guard handoffs rather than only file presence.

## Docs

Canonical project-state files must stay current when state changes.
