# Roadmap

## Current Milestone

Phase 11 baseline complete: the repository now prefers direct role-owned canonical `review-pass` writeback for Product, Architect, and Reviewer, keeps `review-result` as a compatibility path, and keeps lead-managed review-gate synthesis plus deterministic fallback in place.

## Later Milestones

- automate more canonical state updates, handoff generation, and decision gating
- expand validation from structural checks into behavior-aware orchestration checks
- explore richer live review and build orchestration on top of canonical role-owned artifacts
