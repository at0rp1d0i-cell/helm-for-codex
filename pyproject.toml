[project]
name = "helm4codex"
version = "0.1.0"
description = "A repo-backed AI engineering team for Codex"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
  "pytest>=8.4.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
